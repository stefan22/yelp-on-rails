<% if Restaurant.all.any? %>
  <% @restaurants.each do | restaurant | %>
    <h2><%= link_to restaurant.name, restaurant_path(restaurant) %></h2>
    <span><%= link_to "Edit #{restaurant.name}", edit_restaurant_path(restaurant) %></span>
    <span style="margin-left:20px;"><%= link_to "Delete #{restaurant.name}", restaurant_path(restaurant), method: :delete %></span>
    <span style="margin-left:20px;">
    <%= link_to "Review #{restaurant.name}", restaurant_path(restaurant) %>
    </span>
  <% end %>
<% else %>
  <h1>No restaurants yet</h1>
<% end %>
<div style="margin-top:2.5em; border:1px solid gray; padding:1em; width:110px;text-align:center;
            border-radius:5px;background-color:#f4f4f4;">
<a href="/restaurants/new">Add a restaurant</a>
</div>
